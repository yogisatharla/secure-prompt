import React from 'react';

export const Footer = () => {
  return (
    <footer className="w-full py-4 text-center text-[10px] font-mono text-brand-muted">
      Regex → Microsoft Presidio → spaCy NER → Redaction → Phi-4-mini → Safe Prompt
    </footer>
  );
};
